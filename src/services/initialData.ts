import { KmzLayer, RiskPoint, ComunaIncident } from '../types';

export const DEFAULT_COMUNA_CENTER: [number, number] = [-36.4883, -72.7031];
export const DEFAULT_COMUNA_ZOOM = 12;

// Real user uploaded layers and points only - No mock or demo sectors
export const INITIAL_DEMO_LAYERS: KmzLayer[] = [];

export const INITIAL_RISK_POINTS: RiskPoint[] = [];

export const INITIAL_INCIDENTS: ComunaIncident[] = [];
